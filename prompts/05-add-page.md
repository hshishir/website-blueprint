# Claude Code Prompt: Add a New Page

Read `CLAUDE.md`.

Add a new page using the existing architecture.

Inputs:

- Page name: [REPLACE]
- Route: [REPLACE]
- Purpose: [REPLACE]
- Primary audience: [REPLACE]
- Primary CTA: [REPLACE]
- Content file: `content/[REPLACE].md`

Requirements:

- Content must be sourced from Markdown.
- Reuse layout and motion components.
- Preserve theme and accessibility.
- Add navigation only when explicitly requested.
- Maintain static export.
- Do not invent business facts.

Run lint, type checking, and build.
